const Header = ({ text }) => {
  return (
    <header className="w-full h-28 flex justify-center items-center text-black font-bold text-4xl">
      {text}
    </header>
  );
};

export default Header;
